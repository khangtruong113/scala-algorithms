//import scala.collection.mutable
import scala.collection.immutable.HashMap

val array = Array(1, 2, 3, 4, 5, 4, 12, 23)
//val hash = new mutable.HashMap()

//filename.groupBy(w => (w -> 1) )
//filename.map(w => (w, 1)).foldLeft(HashMap()(

//array.map(x => HashMap((x,1))).foldLeft(HashMap[Int, Int])((key, value) => )

//val hsL HashMap

//array.groupBy(l => (l, 1)).size

//val map = HashMap((1 -> 1))