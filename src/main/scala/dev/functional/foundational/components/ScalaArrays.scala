package dev.functional.foundational.components

object ScalaArrays extends App {
  //  Scala arrays are mutable collections of value and are indexed.
  //  This means we can access any element of an array if we know its index in O(1) running time
  //  Simple example
  val myNumbers = Array(0, 2, 4, 6, 8, 10)
  myNumbers(5)
}
